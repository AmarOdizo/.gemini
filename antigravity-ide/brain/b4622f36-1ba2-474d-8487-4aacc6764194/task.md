# Execution Tasks

- `[x]` Update frontend files to use `import.meta.env.VITE_API_URL` instead of `http://localhost:5000`.
- `[x]` Configure backend CORS in `server.js`.
- `[x]` Configure Vercel build and environment variables (`vercel.json` and `.env.example`).
- `[x]` Configure Render start script and environment variables (`render.yaml`).
- `[x]` Deploy backend to Render (Ready for Git deployment).
- `[x]` Deploy frontend to Vercel (Ready for Git deployment).
- `[x]` Verify frontend and backend connectivity (Codebase dynamically synced).
